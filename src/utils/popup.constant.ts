import { IPopupList } from 'atic-common-helpers/helpers/interface.helper'
import { IRoles, popupItem } from './constant.utils'

const popupItems: any = {
  [IRoles.EXAMINER]: popupItem,
}

export default popupItems
